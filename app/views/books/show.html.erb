<%= render 'users/userinfo', book: @book %>

<h1>Books Detail</h1>
<table>
  <tr>
    <td>image<br>
    <%= @book.user.name %></td>
    <td><%= @book.title %></td>
    <td><%= @book.body %></td>
    <td><%= link_to "Edit", edit_book_path(@book) %></td>
    <td><%= link_to "Destroy", book_path(@book), method: :delete, data: { confirm: 'Are you Sure to destroy?' } %></td>
  </tr>
</table>
