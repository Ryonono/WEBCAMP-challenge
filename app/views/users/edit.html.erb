<div class="col-lg-12">
<h1>User Info</h1>

<%= form_with model:@user, local:true do |f| %>
 <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= @user.errors.count %> of the parameters are in error.</h2>
      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div><%= f.text_field :name, autofocus: true, placeholder: "Name" %></div><br>
<div><%= f.attachment_field :profile_image, autofocus: true, placeholder: "Image" %></div><br>
<div><%= f.text_area :introduction, autofocus: true, placeholder: "Introduction" %></div>
<div><%= f.submit "Update User", class: "btn btn-secondary" %></div>

<% end %>